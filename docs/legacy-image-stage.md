# Legacy Image Stage

This repo briefly contained `Image Stage`, a browser-based slideshow app.

That app was useful for screen sharing, but it could not appear in Zoom, Teams,
Chrome, or FaceTime as a camera device. It used a local browser UI and did not
include a CoreMediaIO Camera Extension.

The current project intentionally replaces that approach with a native macOS
host app and embedded Camera Extension.
